package com.example.livent.presentation.navigation

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/** @deprecated Use [RootNavHost] — kept as alias for compatibility. */
@Composable
fun LiventNavHost(modifier: Modifier = Modifier) {
    RootNavHost(modifier = modifier)
}
