# Supabase — Settings → API (usar solo la clave anon public)
# URL base del proyecto.
supabase.url=https://MI_PROJECT_REF.supabase.co
supabase.anon.key=YOUR_ANON_KEY

# Stripe Test (Dashboard → Developers → API keys / Products).
stripe.publishable.key=
stripe.price.premium=
stripe.price.boost=